题目含义：
Input 一组nums，在一组书中找target数通过相加，return数字的位置，从0开始，已经给了int[] nums, int target.

思路：
限制了两个数字相加，选择position是0的数字然后用遍历的方法去与其他数字相加，全部加完move到下一个数字，直到找到target number.

方法一：Brute Force
遍历每个元素 x x x 并查找是否存在另一个等于 t a r g e t − x 的值

时间复杂度：对于每个元素，我们尝试通过循环遍历数组的其余部分来找到它的补码，这需要 O ( n ) 时间。 因此，时间复杂度为 O(n^2) 
空间复杂度：O（1）所需空间不依赖于输入数组的大小，因此仅使用常量空间。
